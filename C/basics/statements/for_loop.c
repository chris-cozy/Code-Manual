//Basic syntax
for(init ; condition; increment)
{
    statement(s);
}

//Example - This iterates 10 times and prints the value of the iterator
for(int i = 0; i < 10; i++)
{
    printf("Value of the iterator: %d", i);
}